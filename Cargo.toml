[package]
name         = "openid-provider"
version      = "0.0.0"
edition      = "2024"
rust-version = "1.85"
license      = "MIT OR Apache-2.0"
description  = "Crate name reserved for the openid-provider project: a storage-agnostic OpenID Connect provider (OP) library."
repository   = "https://github.com/0baseapp/openid-provider"
keywords     = ["openid", "oidc", "oauth2", "authentication", "wasm"]
categories   = ["authentication", "web-programming"]

[dependencies]
